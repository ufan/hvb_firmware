# CMSIS-DAP probe config (e.g. Raspberry Pi Debug Probe), alternative to J-Link.
# Selected via: west flash -r openocd / west debug -r openocd
source [find interface/cmsis-dap.cfg]
cmsis_dap_vid_pid 0x2e8a 0x000c
transport select swd
adapter speed 4000

source [find target/stm32f1x.cfg]
